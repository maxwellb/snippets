## Template #####
# ; Replace <|variables|>: remote_hostname, remote_ip_address, remote_user, remote_uid
#
# Host <|remote_hostname|>
#         HostName <|remote_ip_address|>
#         User <|remote_user|>
#         RemoteForward /run/user/<|remote_uid|>/waypipe-remote.sock /run/user/%i/waypipe.sock
#         RemoteCommand bash -c 'U="ssh-gui-$$"; WAYPIPE_SOCKET="$(ls --sort=time /run/user/1000/waypipe-wt-*.sock | head -n 1)"; exec systemd-run --user --scope --unit="$U" --setenv=SCOPE_UNIT="$U.scope" --property=MemoryMax=2G --property=MemoryHigh=1.5G dbus-run-session -- waypipe --socket "${WAYPIPE_SOCKET}" --unlink-socket --compress zstd server -- /bin/bash'
#         RequestTTY yes
#         ForwardX11 yes
#         ForwardX11Trusted yes
#         Compression yes
# 
